package com.app.backend.security;

// Legacy placeholder to avoid duplicate class errors. Use `JwtTokenProvider` instead.
public class JwTokenProvider {
    // Intentionally left blank. The project uses `JwtTokenProvider` (correct implementation).
}