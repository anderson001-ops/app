package com.app.backend;

/**
 * Legacy/incorrect main class kept for compatibility during transition.
 * The proper application entry point is `com.app.backend.BackendApplication`
 * located under `src/main/java`.
 */
public class BackendApplicationOld {
    // Intentionally empty. Use the new `BackendApplication` class instead.
}
